.env
# Teslamate auth file
teslamate-auth

# Used by Kubernetes Bootstrapper to create a Kubernetes cluster
# Contains the vCenter credentials
vcenter/infrastructure/vms/kubernetes-bootstrapper/.envs

# Used by Kubernetes Image Builder to build the Kubernetes templates
# Contains the vCenter credentials
vcenter/infrastructure/vms/kubernetes-image-builder/vsphere.json

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sentitive data, such as password, private keys, and other secrets.
# These should not be part of version control as they are data points which are potentially sensitive and subject to change depending on the environment.
*.tfvars

# Exclude Kubernetes admin configuration files
kubernetes/clusters/*.conf
